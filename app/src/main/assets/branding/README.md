# Branding Assets

Use this folder for source artwork that should stay editable.

- `logos/` for brand marks, app artwork, and SVG source files
- `icons/` for future icon source files and alternates

For Android launcher icons or UI icons that the app renders directly, prefer `res/mipmap-*` and `res/drawable-*` outputs rather than source SVGs.